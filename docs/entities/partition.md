# Partition

A **partition** is a division of a set into distinct subsets such that every element of the original 
set is included in exactly one subset. In other words, a partition breaks down a set into 
non-overlapping parts that together cover the entire set.

(especially with reference to a country with separate areas of government) the action or state 
of dividing or being divided into parts.