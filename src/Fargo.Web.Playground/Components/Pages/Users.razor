@page "/users"
@page "/users/{EntityGuid:guid}"
@rendermode InteractiveServer

<PageTitle>Users - Fargo Playground</PageTitle>

<RequirePlaygroundAuth Title="Users">
    <ApiWorkspace Resource="PlaygroundResource.Users" EntityGuid="@EntityGuid" />
</RequirePlaygroundAuth>

@code {
    [Parameter]
    public Guid? EntityGuid { get; set; }
}
